# Опционально — для расширенной аналитики
FRED_API_KEY=
# Glassnode — дозаполнение пропусков после proxy §8.10; нужны USE_GLASSNODE=true и ключ
USE_GLASSNODE=false
GLASSNODE_API_KEY=
COINGLASS_API_KEY=

# Путь к YAML со весами скорера, порогам score→signal, аллокации и весам CoinGecko composite (E2).
# По умолчанию: bit_trend/config/scoring.yaml в установленном пакете.
# BITTREND_SCORING_CONFIG=

# HTTP ко всем внешним API (bit_trend.data.http_client): интервал по хосту, ретраи, backoff
HTTP_RATE_MIN_INTERVAL_SEC=0.12
HTTP_MAX_RETRIES=3
HTTP_BACKOFF_BASE_SEC=0.85
HTTP_BACKOFF_MAX_SEC=32
# Опционально: список кодов для повтора, через запятую (по умолчанию 429,500,502,503,504)
# HTTP_RETRY_STATUS=

# TTL кэша (секунды): быстрый блок — цена, funding/OI, MA200, Fear&Greed; медленный — макро, ончейн, ETF, §8.10 proxy
# CACHE_TTL передаётся в DataFetcher как базовый TTL быстрого блока; пустой CACHE_TTL_FAST = то же, что CACHE_TTL
CACHE_TTL=300
CACHE_TTL_FAST=
CACHE_TTL_SLOW=3600

# --- plan01 §12: рыночные данные (primary / fallback / TTL / устойчивость) ---
# Справочный YAML со всеми ключами в человекочитаемом виде: bit_trend/config/market_data.example.yaml
# (приложение использует переменные окружения ниже, не парсит YAML автоматически.)
#
# FreeCryptoAPI — price / market_cap / volume (plan01); token: https://freecryptoapi.com/
# Без токена цепочка MARKET_DATA_* уходит на fallback (binance, coingecko).
# FREECRYPTO_API_TOKEN=
# FREECRYPTO_API_BASE=https://api.freecryptoapi.com/v1
# MARKET_DATA_PRIMARY=freecrypto
# MARKET_DATA_FALLBACK=binance,coingecko
# plan01 §10: кэш «текущего рынка» в get_market_current_with_fallback (сек, 300–900)
# MARKET_CURRENT_CACHE_TTL_SEC=600
# plan01 §9: попытки на один источник при транзиентных сбоях; база backoff (сек)
# MARKET_SOURCE_MAX_ATTEMPTS=3
# MARKET_SOURCE_RETRY_BASE_SEC=0.35
# Опциональный circuit breaker по имени провайдера (после N транзиентных провалов — пауза)
# MARKET_CIRCUIT_BREAKER=false
# MARKET_CB_FAILURE_THRESHOLD=5
# MARKET_CB_OPEN_SEC=60

# Сверка текущей цены (и капа/объёма, если оба ответа полные) FreeCrypto vs CoinGecko — plan01 §11.2.
# Включите для локального/CI прогона: pytest -m integration (нужны сеть + FREECRYPTO_API_TOKEN;
# для CoinGecko желателен COINGECKO_DEMO_API_KEY или COINGECKO_PRO_API_KEY при лимитах/401).
# COINGECKO_VERIFY=false

# Proxy §8.10 (MVRV/NUPL/SOPR + composite): ряд build_market_history (FreeCrypto + SQLite market_data).
# USE_COINGECKO_ONCHAIN — включить расчёт (имя историческое; CoinGecko market_chart для него не используется).
USE_COINGECKO_ONCHAIN=true
COINGECKO_ONCHAIN_CONFIDENCE=0.55
COINGECKO_ONCHAIN_SOURCE_SCORE=0.52
# Глубина build_market_history для proxy (дней, макс. 3650 как у FreeCrypto getHistory)
# ONCHAIN_PROXY_HISTORY_DAYS=3650
# Минимум строк price+cap (по умолчанию 400); иначе proxy не строится — снимки market_data (plan01 §4.1)
# ONCHAIN_PROXY_MIN_ROWS=400
# Если в ряде нет market_cap (редкие снимки): кап ≈ price × оценка circulating supply BTC
# ONCHAIN_PROXY_BTC_SUPPLY_EST=19500000
# Кэш результата §8.10 (сек)
COINGECKO_BUNDLE_CACHE_SEC=120
# CoinGecko — fallback в MARKET_DATA_* / get_history у CoinGeckoMarketDataSource (market_chart при необходимости)
# COINGECKO_DEMO_API_KEY=
# COINGECKO_PRO_API_KEY=
# Опционально: офлайн-ряд для notebooks/backtest_onchain_proxy.ipynb (колонки как у get_coingecko_810_dataframe)
# BITTREND_BACKTEST_CG_CSV=
# Ниже — необязательные переопределения чисел из scoring.yaml (приоритет над YAML).
# Веса composite_onchain (z-сумма); W_VOLATILITY обычно отрицательный
COMPOSITE_810_W_MVRV=0.30
COMPOSITE_810_W_NUPL=0.25
COMPOSITE_810_W_SOPR=0.20
COMPOSITE_810_W_DRAWDOWN=0.25
COMPOSITE_810_W_VOLATILITY=-0.10
COMPOSITE_810_Z_WINDOW=365
COMPOSITE_810_Z_MIN_PERIODS=30
# Опционально смешивать composite в BitTrendScorer (0 = только UI, см. upgrade_plan S1)
SCORER_WEIGHT_COMPOSITE_810=0
SCORER_COMPOSITE_810_SCALE=40

# LookIntoBitcoin — кэш (86400), выходные 2x
LOOKINTOBITCOIN_CACHE_TTL=86400
# Circuit breaker: отключить на N часов после M неудач
LOOKINTOBITCOIN_FAIL_LIMIT=5
LOOKINTOBITCOIN_DISABLE_HOURS=6
# Стабильность: max_delta для защиты от скачков
LOOKINTOBITCOIN_MAX_DELTA_MVRV=0.5
LOOKINTOBITCOIN_MAX_DELTA_NUPL=0.1
LOOKINTOBITCOIN_MAX_DELTA_SOPR=0.1
# Freshness, source_score
LOOKINTOBITCOIN_MAX_AGE_HOURS=24
LOOKINTOBITCOIN_SOURCE_SCORE_THRESHOLD=0.4
# Feature flags — дозаполнение после proxy §8.10 (парсинг сайта)
USE_LOOKINTOBITCOIN=false
# USE_SELENIUM=false — ускорит загрузку (парсинг LookIntoBitcoin без Chrome)
USE_SELENIUM=true
# SQLite storage (market_data — локальные снимки рынка при неполной истории API, plan01 §4)
BITTREND_DB_PATH=
# Ежедневный снимок в market_data: планировщик задач Windows минимум 1×/сутки, например (PowerShell):
# python -c "from bit_trend.data.market_source import collect_daily_snapshot; collect_daily_snapshot('BTC', min_interval_hours=20)"
# История сигналов UI (P1): дубль строк в CSV при каждой новой записи в БД
# BITTREND_SIGNAL_CSV_PATH=data/signal_history.csv
# Не писать повтор в БД/CSV, если тот же score/signal/портфель в окне (сек); 0 = отключить дедуп
# BITTREND_SIGNAL_DEDUPE_SEC=90

# Дрейф ончейн-ряда в SQLite (S3 upgrade_plan): алерт + умножение весов MVRV/NUPL/SOPR при флаге
# ONCHAIN_DRIFT_ENABLED=true
# ONCHAIN_DRIFT_HISTORY_LIMIT=500
# ONCHAIN_DRIFT_WINDOW=10
# ONCHAIN_DRIFT_WEIGHT_FACTOR=0.25
# Подстрока source в onchain_history; пусто = все строки
# ONCHAIN_DRIFT_SOURCE_SUBSTRING=lookintobitcoin
# ONCHAIN_DRIFT_THRESHOLD_MVRV=0.5
# ONCHAIN_DRIFT_THRESHOLD_NUPL=0.12
# ONCHAIN_DRIFT_THRESHOLD_SOPR=0.12

# --- P3: реальное исполнение (ccxt) — только после стабильного скоринга; по умолчанию MVP-логирование ---
# pip install ccxt>=4.0.0 (см. requirements.txt)
# BITTREND_LIVE_TRADING=true
# Обязательное подтверждение (защита от случайного включения): ровно YES
# BITTREND_LIVE_TRADING_ACK=YES
# BITTREND_CCXT_EXCHANGE=binance
# BITTREND_CCXT_SYMBOL=BTC/USDT
# BITTREND_CCXT_API_KEY=
# BITTREND_CCXT_API_SECRET=
# Для бирж, где нужен passphrase (часть futures)
# BITTREND_CCXT_PASSWORD=
# Песочница, если биржа поддерживает через ccxt
# BITTREND_CCXT_TESTNET=false
# При ончейн-дрейфе не слать live-ордера (по умолчанию true)
# BITTREND_LIVE_BLOCK_ON_DRIFT=true
